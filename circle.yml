machine:
  python:
    version: 2.7.9
  environment:
    PYENV_VERSION: 2.7.9
    GT_ENVIRONMENT: circle

dependencies:
   override:
    - pip install -r AuthService/requirements.txt

test:
  override:
    - py.test AuthService/tests/:
        parallel: true
