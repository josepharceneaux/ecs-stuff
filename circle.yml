machine:
  python:
    version: 2.7.9
  environment:
    PYENV_VERSION: 2.7.9
    GT_ENVIRONMENT: circle

dependencies:
   override:
    - pip install -r requirements.txt

test:
  override:
    - py.test auth_service/tests/:
        parallel: true
    - py.test resume_service/tests/:
        parallel: true
    - py.test activities_service/tests/:
        parallel: true
