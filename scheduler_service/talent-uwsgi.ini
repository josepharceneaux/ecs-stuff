[uwsgi]

module = run
callable = app
master = true
processes = 5

attach-daemon = python run_celery.py
attach-daemon = python run_flower.py

socket = /tmp/talent-uwsgi.sock
chown-socket = www-data:www-data

vacuum = true
enable-threads=true
single-interpreter = true
die-on-term = true