"""
Truncate existing document table and add a column for keypath
"""


from sqlalchemy import text
from candidate_service.common.models.db import db


db.session.execute(text("TRUNCATE candidate_document"))
db.session.commit()
db.session.execute(
    text("ALTER TABLE `candidate_document` ADD KeyPath VARCHAR(255);")
)
db.session.commit()